// b is a string with only uppercase
// a is a string with both upperccase
const isUooerCase = char => {
  const code = char.charCodeAt();
  if (code >= 97 && code <= 122) return 1;
  else return -1;
};
const abbrevation = (a, b) => {

};

const helper = (a, b, ia, ib) => {
  if(a.length < b.length) return
  // case to check on the remaining characters 
  //o see whether the remaining characters are lower case or upper case
  // create a variable s which is a string k
  // write the base case of this recursive function
  //look at the first characters of both strings
  //and and see whether they are the same if capilised a character
  // st
  // case to hundle 
};
//a-97, A-65
// z-122,Z-90

console.log(isUooerCase("f"));
